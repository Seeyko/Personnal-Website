services:
  frontend:
    env_file:
      - .env
    build:
      context: .
      dockerfile: frontend/Dockerfile
      target: production
      args:
        - API_URL=${API_URL:-https://api.tomandrieu.com}
        - FRONTEND_URL=${FRONTEND_URL:-https://tomandrieu.com}
        - ADMIN_SECRET_PATH=${ADMIN_SECRET_PATH:-_mx9k7}
    image: ${CI_REGISTRY_IMAGE}/frontend:${APP_VERSION:-latest}
    expose:
      - ${FRONTEND_PORT:-4200}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tomandrieu-frontend.rule=Host(`tomandrieu.com`)"
      - "traefik.http.routers.tomandrieu-frontend.priority=1"
      - "traefik.http.routers.tomandrieu-frontend.entrypoints=websecure"
      - "traefik.http.routers.tomandrieu-frontend.tls.certResolver=letsencrypt"
      - "traefik.http.services.tomandrieu-frontend.loadbalancer.server.port=4200"
    networks:
      - dokploy-network

  backend:
    env_file:
      - .env
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: ${CI_REGISTRY_IMAGE}/backend:${APP_VERSION:-latest}
    expose:
      - ${BACKEND_PORT:-3000}
    environment:
      - PORT=${BACKEND_PORT:-3000}
      - ARTICLES_DIR=/app/articles
      - FRONTEND_URL=${FRONTEND_URL:-https://tomandrieu.com}
      - API_URL=${API_URL:-https://api.tomandrieu.com}
      - DATABASE_URL=${DATABASE_URL}
      - ADMIN_API_KEY=${ADMIN_API_KEY}
      - ADMIN_SECRET_PATH=${ADMIN_SECRET_PATH}
      - ADMIN_ALLOWED_IPS=${ADMIN_ALLOWED_IPS:-}
      - POSTHOG_API_KEY=${POSTHOG_API_KEY:-}
      - POSTHOG_HOST=${POSTHOG_HOST:-https://eu.i.posthog.com}
    labels:
      - "traefik.enable=true"
      # API subdomain
      - "traefik.http.routers.tomandrieu-api.rule=Host(`api.tomandrieu.com`)"
      - "traefik.http.routers.tomandrieu-api.entrypoints=websecure"
      - "traefik.http.routers.tomandrieu-api.tls.certResolver=letsencrypt"
      - "traefik.http.services.tomandrieu-api.loadbalancer.server.port=3000"
      # SSR blog routes on main domain (priority 10 > frontend priority 1)
      - "traefik.http.routers.tomandrieu-ssr.rule=Host(`tomandrieu.com`) && (Path(`/blog`) || PathPrefix(`/blog/`))"
      - "traefik.http.routers.tomandrieu-ssr.priority=10"
      - "traefik.http.routers.tomandrieu-ssr.entrypoints=websecure"
      - "traefik.http.routers.tomandrieu-ssr.tls.certResolver=letsencrypt"
      - "traefik.http.routers.tomandrieu-ssr.service=tomandrieu-api"
      # SEO routes (sitemap, robots) (priority 10 > frontend priority 1)
      - "traefik.http.routers.tomandrieu-seo.rule=Host(`tomandrieu.com`) && (Path(`/sitemap.xml`) || Path(`/robots.txt`))"
      - "traefik.http.routers.tomandrieu-seo.priority=10"
      - "traefik.http.routers.tomandrieu-seo.entrypoints=websecure"
      - "traefik.http.routers.tomandrieu-seo.tls.certResolver=letsencrypt"
      - "traefik.http.routers.tomandrieu-seo.service=tomandrieu-api"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
